server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/library
    username: postgres
    password: secret
  jpa:
    hibernate:
      ddl-auto: update


  devtools:
    add-properties: true

